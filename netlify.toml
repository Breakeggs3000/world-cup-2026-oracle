# Netlify alternative to Vercel for the React frontend
[build]
  command = "npm ci && npm run build"
  publish = "dist"
  base = "src/frontend"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
